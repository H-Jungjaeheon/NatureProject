using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SolarHeatUnit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //화면 스크롤 적 성 만들기
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
